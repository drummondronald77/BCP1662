FIXED SINGLE-FILE EDITION
Upload index.html plus the two icon PNGs and manifest.webmanifest to the ROOT of your GitHub Pages repository. Replace the old files. index.html now contains the app code, styles, lectionary data, office text, and Litany internally, so missing JS/CSS files cannot leave the page empty.
